CREATE TABLE IF NOT EXISTS Exercise (
    id INT NOT NULL,
    nombre_ejercicio VARCHAR (250) NOT NULL,
    repeticion INT NOT NULL,
    serie INT NOT NULL,
    peso INT NOT NULL,
    PRIMARY KEY (id)
);
